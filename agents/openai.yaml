interface:
  display_name: "OfficePilot AI"
  short_description: "Decision execution risk and dashboard agent."
  default_prompt: "Use $officepilot-ai to analyze meeting notes, weekly reports, emails, or task records into decisions, tasks, owners, risks, reminders, and an executive dashboard."
policy:
  allow_implicit_invocation: true
